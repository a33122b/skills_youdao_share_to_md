interface:
  display_name: "有道分享页转 Markdown"
  short_description: "自动导出有道分享链接"
  default_prompt: "用户提供了一个完整的有道分享链接。请将其导出为 Markdown，并返回生成文件和资源的可点击链接。优先使用中文，能用脚本就不要手写流程。"

policy:
  allow_implicit_invocation: true
